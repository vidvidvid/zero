{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. core:window:default grants allow-internal-toggle-maximize but not allow-start-dragging, so without the latter a data-tauri-drag-region titlebar zooms on double-click while every drag is rejected at the IPC.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:webview:allow-set-webview-zoom",
    "dialog:default",
    "core:window:allow-start-dragging",
    "updater:default",
    "process:allow-restart"
  ]
}
