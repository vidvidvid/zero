<!-- ZERO.md — zero's setup file for this project, one per repository,
     the way a CLAUDE.md is. It holds one thing so far: the vocabulary
     this project's voice memos are transcribed and cleaned up against.
     One term per line: "- Term — notes about how transcription mangles it".
     The term is fed to speech recognition; the whole line guides cleanup.
     Meant to be committed, so the project's words travel with the repo. -->
- zero — this project's name; restore it exactly as written here if the transcription mangles it.
- TRMNL — pronounced exactly like "terminal"; will transcribe as that ordinary word
- ZERO.md — heard as "zero dot MD" or "0.md"; the per-project vocabulary file
- Anthropic — commonly mangled to "entropic" or "anthropic" mis-stressed
- Tauri — sounds like "Tori", "Taury", "Torrey"
- xterm.js — heard as "X term JS", "extermJS"
- WKWebView — "WK web view", "W K WebView", "double-you-kay"
- CodeMirror — split into "code mirror" as two ordinary words
- ProMotion — heard as "promotion", the ordinary word
- ripgrep — "rip grep" as two words, or "rip-grip"
- portable-pty — "pty" heard as "P T Y", "pity", or "putty"
- Cursor — the editor; indistinguishable from the ordinary word "cursor"
- WebKit — "web kit" as two words
- Claude Code — "Claude" heard as "cloud", "clod", "Claud"
- rustup.rs — "rust up dot RS", "rust-up-dot-R-S"
- actool — "A-C-tool", "act tool", "actual"
- ibtoold — "IB tool D", "IB told"; the daemon name ends in a d
- Assets.car — "assets car"; sounds like a vehicle, not a compiled catalog
- ZDOTDIR — "Z dot dir", "zee dot D I R"
- swiftc — "Swift C", "Swift see"; the compiler, not the language
- macOS — misheard as: macro I said (suggested)
- Dynamic Island — misheard as: dynamic island (suggested)
